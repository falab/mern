<div class="mainContainer">
  <div class="menuContainer">
    <ul class="menuList">
      {{#each menuItems}}
        <li><a href="{{path}}"{{#if remote}} target="_blank"{{/if}}>{{text}}</a></li>
      {{/each}}
    </ul>
  </div>
</div>
